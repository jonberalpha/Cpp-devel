#pragma once

#include <string>

/**
 * Computes the pascals triangle according to a given number between 1 and 10, 
 * and returns a string which can be printed via cout
 * Input:  (integer) number between 1 to 10
 * Output:  (string) string of the computed pascals triangle
 */
std::string pascal(int number);

